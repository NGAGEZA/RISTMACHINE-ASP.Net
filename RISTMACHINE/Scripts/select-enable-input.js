$(document).ready(function() {
    var $selectImp11 = $("[id$='select_imp1_1']"),
        $tbImp11 = $("[id$='tb_imp1_1']");
       
    $selectImp11.change(function () {
        
        if ($selectImp11.val() === "2") {
            $tbImp11.removeAttr("disabled");
            $tbImp11.focus();
        } else {
            $tbImp11.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr11 = $("[id$='select_str1_1']"),
        $tbStr11 = $("[id$='tb_str1_1']");

    $selectStr11.change(function () {

        if ($selectStr11.val() === "2") {
            $tbStr11.removeAttr("disabled");
            $tbStr11.focus();
        } else {
            $tbStr11.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp12 = $("[id$='select_imp1_2']"),
        $tbImp12 = $("[id$='tb_imp1_2']");

    $selectImp12.change(function () {

        if ($selectImp12.val() === "2") {
            $tbImp12.removeAttr("disabled");
            $tbImp12.focus();
        } else {
            $tbImp12.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr12 = $("[id$='select_str1_2']"),
        $tbStr12 = $("[id$='tb_str1_2']");

    $selectStr12.change(function () {

        if ($selectStr12.val() === "2") {
            $tbStr12.removeAttr("disabled");
            $tbStr12.focus();
        } else {
            $tbStr12.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp1A = $("[id$='select_imp1_A']"),
        $tbImp1A = $("[id$='tb_imp1_A']");

    $selectImp1A.change(function () {

        if ($selectImp1A.val() === "2") {
            $tbImp1A.removeAttr("disabled");
            $tbImp1A.focus();
        } else {
            $tbImp1A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr1A = $("[id$='select_str1_A']"),
        $tbStr1A = $("[id$='tb_str1_A']");

    $selectStr1A.change(function () {

        if ($selectStr1A.val() === "2") {
            $tbStr1A.removeAttr("disabled");
            $tbStr1A.focus();
        } else {
            $tbStr1A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp13 = $("[id$='select_imp1_3']"),
        $tbImp13 = $("[id$='tb_imp1_3']");

    $selectImp13.change(function () {

        if ($selectImp13.val() === "2") {
            $tbImp13.removeAttr("disabled");
            $tbImp13.focus();
        } else {
            $tbImp13.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr13 = $("[id$='select_str1_3']"),
        $tbStr13 = $("[id$='tb_str1_3']");

    $selectStr13.change(function () {

        if ($selectStr13.val() === "2") {
            $tbStr13.removeAttr("disabled");
            $tbStr13.focus();
        } else {
            $tbStr13.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp14 = $("[id$='select_imp1_4']"),
        $tbImp14 = $("[id$='tb_imp1_4']");

    $selectImp14.change(function () {

        if ($selectImp14.val() === "2") {
            $tbImp14.removeAttr("disabled");
            $tbImp14.focus();
        } else {
            $tbImp14.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr14 = $("[id$='select_str1_4']"),
        $tbStr14 = $("[id$='tb_str1_4']");

    $selectStr14.change(function () {

        if ($selectStr14.val() === "2") {
            $tbStr14.removeAttr("disabled");
            $tbStr14.focus();
        } else {
            $tbStr14.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp15 = $("[id$='select_imp1_5']"),
        $tbImp15 = $("[id$='tb_imp1_5']");

    $selectImp15.change(function () {

        if ($selectImp15.val() === "2") {
            $tbImp15.removeAttr("disabled");
            $tbImp15.focus();
        } else {
            $tbImp15.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr15 = $("[id$='select_str1_5']"),
        $tbStr15 = $("[id$='tb_str1_5']");

    $selectStr15.change(function () {

        if ($selectStr15.val() === "2") {
            $tbStr15.removeAttr("disabled");
            $tbStr15.focus();
        } else {
            $tbStr15.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp16 = $("[id$='select_imp1_6']"),
        $tbImp16 = $("[id$='tb_imp1_6']");

    $selectImp16.change(function () {

        if ($selectImp16.val() === "2") {
            $tbImp16.removeAttr("disabled");
            $tbImp16.focus();
        } else {
            $tbImp16.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr16 = $("[id$='select_str1_6']"),
        $tbStr16 = $("[id$='tb_str1_6']");

    $selectStr16.change(function () {

        if ($selectStr16.val() === "2") {
            $tbStr16.removeAttr("disabled");
            $tbStr16.focus();
        } else {
            $tbStr16.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp17 = $("[id$='select_imp1_7']"),
        $tbImp17 = $("[id$='tb_imp1_7']");

    $selectImp17.change(function () {

        if ($selectImp17.val() === "2") {
            $tbImp17.removeAttr("disabled");
            $tbImp17.focus();
        } else {
            $tbImp17.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr17 = $("[id$='select_str1_7']"),
        $tbStr17 = $("[id$='tb_str1_7']");

    $selectStr17.change(function () {

        if ($selectStr17.val() === "2") {
            $tbStr17.removeAttr("disabled");
            $tbStr17.focus();
        } else {
            $tbStr17.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp18 = $("[id$='select_imp1_8']"),
        $tbImp18 = $("[id$='tb_imp1_8']");

    $selectImp18.change(function () {

        if ($selectImp18.val() === "2") {
            $tbImp18.removeAttr("disabled");
            $tbImp18.focus();
        } else {
            $tbImp18.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr18 = $("[id$='select_str1_8']"),
        $tbStr18 = $("[id$='tb_str1_8']");

    $selectStr18.change(function () {

        if ($selectStr18.val() === "2") {
            $tbStr18.removeAttr("disabled");
            $tbStr18.focus();
        } else {
            $tbStr18.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp19 = $("[id$='select_imp1_9']"),
        $tbImp19 = $("[id$='tb_imp1_9']");

    $selectImp19.change(function () {

        if ($selectImp19.val() === "2") {
            $tbImp19.removeAttr("disabled");
            $tbImp19.focus();
        } else {
            $tbImp19.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr19 = $("[id$='select_str1_9']"),
        $tbStr19 = $("[id$='tb_str1_9']");

    $selectStr19.change(function () {

        if ($selectStr19.val() === "2") {
            $tbStr19.removeAttr("disabled");
            $tbStr19.focus();
        } else {
            $tbStr19.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp110 = $("[id$='select_imp1_10']"),
        $tbImp110 = $("[id$='tb_imp1_10']");

    $selectImp110.change(function () {

        if ($selectImp110.val() === "2") {
            $tbImp110.removeAttr("disabled");
            $tbImp110.focus();
        } else {
            $tbImp110.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr110 = $("[id$='select_str1_10']"),
        $tbStr110 = $("[id$='tb_str1_10']");

    $selectStr110.change(function () {

        if ($selectStr110.val() === "2") {
            $tbStr110.removeAttr("disabled");
            $tbStr110.focus();
        } else {
            $tbStr110.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp111 = $("[id$='select_imp1_11']"),
        $tbImp111 = $("[id$='tb_imp1_11']");

    $selectImp111.change(function () {

        if ($selectImp111.val() === "2") {
            $tbImp111.removeAttr("disabled");
            $tbImp111.focus();
        } else {
            $tbImp111.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr111 = $("[id$='select_str1_11']"),
        $tbStr111 = $("[id$='tb_str1_11']");

    $selectStr111.change(function () {

        if ($selectStr111.val() === "2") {
            $tbStr111.removeAttr("disabled");
            $tbStr111.focus();
        } else {
            $tbStr111.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp121 = $("[id$='select_imp12_1']"),
        $tbImp121 = $("[id$='tb_imp12_1']");

    $selectImp121.change(function () {

        if ($selectImp121.val() === "2") {
            $tbImp121.removeAttr("disabled");
            $tbImp121.focus();
        } else {
            $tbImp121.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr121 = $("[id$='select_str12_1']"),
        $tbStr121 = $("[id$='tb_str12_1']");

    $selectStr121.change(function () {

        if ($selectStr121.val() === "2") {
            $tbStr121.removeAttr("disabled");
            $tbStr121.focus();
        } else {
            $tbStr121.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp122 = $("[id$='select_imp12_2']"),
        $tbImp122 = $("[id$='tb_imp12_2']");

    $selectImp122.change(function () {

        if ($selectImp122.val() === "2") {
            $tbImp122.removeAttr("disabled");
            $tbImp122.focus();
        } else {
            $tbImp122.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr122 = $("[id$='select_str12_2']"),
        $tbStr122 = $("[id$='tb_str12_2']");

    $selectStr122.change(function () {

        if ($selectStr122.val() === "2") {
            $tbStr122.removeAttr("disabled");
            $tbStr122.focus();
        } else {
            $tbStr122.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp123 = $("[id$='select_imp12_3']"),
        $tbImp123 = $("[id$='tb_imp12_3']");

    $selectImp123.change(function () {

        if ($selectImp123.val() === "2") {
            $tbImp123.removeAttr("disabled");
            $tbImp123.focus();
        } else {
            $tbImp123.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr123 = $("[id$='select_str12_3']"),
        $tbStr123 = $("[id$='tb_str12_3']");

    $selectStr123.change(function () {

        if ($selectStr123.val() === "2") {
            $tbStr123.removeAttr("disabled");
            $tbStr123.focus();
        } else {
            $tbStr123.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp124= $("[id$='select_imp12_4']"),
        $tbImp124 = $("[id$='tb_imp12_4']");

    $selectImp124.change(function () {

        if ($selectImp124.val() === "2") {
            $tbImp124.removeAttr("disabled");
            $tbImp124.focus();
        } else {
            $tbImp124.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr124 = $("[id$='select_str12_4']"),
        $tbStr124 = $("[id$='tb_str12_4']");

    $selectStr124.change(function () {

        if ($selectStr124.val() === "2") {
            $tbStr124.removeAttr("disabled");
            $tbStr124.focus();
        } else {
            $tbStr124.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp125 = $("[id$='select_imp12_5']"),
        $tbImp125 = $("[id$='tb_imp12_5']");

    $selectImp125.change(function () {

        if ($selectImp125.val() === "2") {
            $tbImp125.removeAttr("disabled");
            $tbImp125.focus();
        } else {
            $tbImp125.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr125 = $("[id$='select_str12_5']"),
        $tbStr125 = $("[id$='tb_str12_5']");

    $selectStr125.change(function () {

        if ($selectStr125.val() === "2") {
            $tbStr125.removeAttr("disabled");
            $tbStr125.focus();
        } else {
            $tbStr125.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp126 = $("[id$='select_imp12_6']"),
        $tbImp126 = $("[id$='tb_imp12_6']");

    $selectImp126.change(function () {

        if ($selectImp126.val() === "2") {
            $tbImp126.removeAttr("disabled");
            $tbImp126.focus();
        } else {
            $tbImp126.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr126 = $("[id$='select_str12_6']"),
        $tbStr126 = $("[id$='tb_str12_6']");

    $selectStr126.change(function () {

        if ($selectStr126.val() === "2") {
            $tbStr126.removeAttr("disabled");
            $tbStr126.focus();
        } else {
            $tbStr126.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp127 = $("[id$='select_imp12_7']"),
        $tbImp127 = $("[id$='tb_imp12_7']");

    $selectImp127.change(function () {

        if ($selectImp127.val() === "2") {
            $tbImp127.removeAttr("disabled");
            $tbImp127.focus();
        } else {
            $tbImp127.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr127 = $("[id$='select_str12_7']"),
        $tbStr127 = $("[id$='tb_str12_7']");

    $selectStr127.change(function () {

        if ($selectStr127.val() === "2") {
            $tbStr127.removeAttr("disabled");
            $tbStr127.focus();
        } else {
            $tbStr127.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp21 = $("[id$='select_imp2_1']"),
        $tbImp21 = $("[id$='tb_imp2_1']");

    $selectImp21.change(function () {

        if ($selectImp21.val() === "2") {
            $tbImp21.removeAttr("disabled");
            $tbImp21.focus();
        } else {
            $tbImp21.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr21 = $("[id$='select_str2_1']"),
        $tbStr21 = $("[id$='tb_str2_1']");

    $selectStr21.change(function () {

        if ($selectStr21.val() === "2") {
            $tbStr21.removeAttr("disabled");
            $tbStr21.focus();
        } else {
            $tbStr21.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp22 = $("[id$='select_imp2_2']"),
        $tbImp22 = $("[id$='tb_imp2_2']");

    $selectImp22.change(function () {

        if ($selectImp22.val() === "2") {
            $tbImp22.removeAttr("disabled");
            $tbImp22.focus();
        } else {
            $tbImp22.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr22 = $("[id$='select_str2_2']"),
        $tbStr22 = $("[id$='tb_str2_2']");

    $selectStr22.change(function () {

        if ($selectStr22.val() === "2") {
            $tbStr22.removeAttr("disabled");
            $tbStr22.focus();
        } else {
            $tbStr22.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp23 = $("[id$='select_imp2_3']"),
        $tbImp23 = $("[id$='tb_imp2_3']");

    $selectImp23.change(function () {

        if ($selectImp23.val() === "2") {
            $tbImp23.removeAttr("disabled");
            $tbImp23.focus();
        } else {
            $tbImp23.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr23 = $("[id$='select_str2_3']"),
        $tbStr23 = $("[id$='tb_str2_3']");

    $selectStr23.change(function () {

        if ($selectStr23.val() === "2") {
            $tbStr23.removeAttr("disabled");
            $tbStr23.focus();
        } else {
            $tbStr23.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp24 = $("[id$='select_imp2_4']"),
        $tbImp24 = $("[id$='tb_imp2_4']");

    $selectImp24.change(function () {

        if ($selectImp24.val() === "2") {
            $tbImp24.removeAttr("disabled");
            $tbImp24.focus();
        } else {
            $tbImp24.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr24 = $("[id$='select_str2_4']"),
        $tbStr24 = $("[id$='tb_str2_4']");

    $selectStr24.change(function () {

        if ($selectStr24.val() === "2") {
            $tbStr24.removeAttr("disabled");
            $tbStr24.focus();
        } else {
            $tbStr24.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp25 = $("[id$='select_imp2_5']"),
        $tbImp25 = $("[id$='tb_imp2_5']");

    $selectImp25.change(function () {

        if ($selectImp25.val() === "2") {
            $tbImp25.removeAttr("disabled");
            $tbImp25.focus();
        } else {
            $tbImp25.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr25 = $("[id$='select_str2_5']"),
        $tbStr25 = $("[id$='tb_str2_5']");

    $selectStr25.change(function () {

        if ($selectStr25.val() === "2") {
            $tbStr25.removeAttr("disabled");
            $tbStr25.focus();
        } else {
            $tbStr25.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp311 = $("[id$='select_imp31_1']"),
        $tbImp311 = $("[id$='tb_imp31_1']");

    $selectImp311.change(function () {

        if ($selectImp311.val() === "2") {
            $tbImp311.removeAttr("disabled");
            $tbImp311.focus();
        } else {
            $tbImp311.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr311 = $("[id$='select_str31_1']"),
        $tbStr311 = $("[id$='tb_str31_1']");

    $selectStr311.change(function () {

        if ($selectStr311.val() === "2") {
            $tbStr311.removeAttr("disabled");
            $tbStr311.focus();
        } else {
            $tbStr311.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp312 = $("[id$='select_imp31_2']"),
        $tbImp312 = $("[id$='tb_imp31_2']");

    $selectImp312.change(function () {

        if ($selectImp312.val() === "2") {
            $tbImp312.removeAttr("disabled");
            $tbImp312.focus();
        } else {
            $tbImp312.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr312 = $("[id$='select_str31_2']"),
        $tbStr312 = $("[id$='tb_str31_2']");

    $selectStr312.change(function () {

        if ($selectStr312.val() === "2") {
            $tbStr312.removeAttr("disabled");
            $tbStr312.focus();
        } else {
            $tbStr312.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp313 = $("[id$='select_imp31_3']"),
        $tbImp313 = $("[id$='tb_imp31_3']");

    $selectImp313.change(function () {

        if ($selectImp313.val() === "2") {
            $tbImp313.removeAttr("disabled");
            $tbImp313.focus();
        } else {
            $tbImp313.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr313 = $("[id$='select_str31_3']"),
        $tbStr313 = $("[id$='tb_str31_3']");

    $selectStr313.change(function () {

        if ($selectStr313.val() === "2") {
            $tbStr313.removeAttr("disabled");
            $tbStr313.focus();
        } else {
            $tbStr313.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp314 = $("[id$='select_imp31_4']"),
        $tbImp314 = $("[id$='tb_imp31_4']");

    $selectImp314.change(function () {

        if ($selectImp314.val() === "2") {
            $tbImp314.removeAttr("disabled");
            $tbImp314.focus();
        } else {
            $tbImp314.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr314 = $("[id$='select_str31_4']"),
        $tbStr314 = $("[id$='tb_str31_4']");

    $selectStr314.change(function () {

        if ($selectStr314.val() === "2") {
            $tbStr314.removeAttr("disabled");
            $tbStr314.focus();
        } else {
            $tbStr314.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp315 = $("[id$='select_imp31_5']"),
        $tbImp315 = $("[id$='tb_imp31_5']");

    $selectImp315.change(function () {

        if ($selectImp315.val() === "2") {
            $tbImp315.removeAttr("disabled");
            $tbImp315.focus();
        } else {
            $tbImp315.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr315 = $("[id$='select_str31_5']"),
        $tbStr315 = $("[id$='tb_str31_5']");

    $selectStr315.change(function () {

        if ($selectStr315.val() === "2") {
            $tbStr315.removeAttr("disabled");
            $tbStr315.focus();
        } else {
            $tbStr315.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp316 = $("[id$='select_imp31_6']"),
        $tbImp316 = $("[id$='tb_imp31_6']");

    $selectImp316.change(function () {

        if ($selectImp316.val() === "2") {
            $tbImp316.removeAttr("disabled");
            $tbImp316.focus();
        } else {
            $tbImp316.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr316 = $("[id$='select_str31_6']"),
        $tbStr316 = $("[id$='tb_str31_6']");

    $selectStr316.change(function () {

        if ($selectStr316.val() === "2") {
            $tbStr316.removeAttr("disabled");
            $tbStr316.focus();
        } else {
            $tbStr316.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp317 = $("[id$='select_imp31_7']"),
        $tbImp317 = $("[id$='tb_imp31_7']");

    $selectImp317.change(function () {

        if ($selectImp317.val() === "2") {
            $tbImp317.removeAttr("disabled");
            $tbImp317.focus();
        } else {
            $tbImp317.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr317 = $("[id$='select_str31_7']"),
        $tbStr317 = $("[id$='tb_str31_7']");

    $selectStr317.change(function () {

        if ($selectStr317.val() === "2") {
            $tbStr317.removeAttr("disabled");
            $tbStr317.focus();
        } else {
            $tbStr317.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp318 = $("[id$='select_imp31_8']"),
        $tbImp318 = $("[id$='tb_imp31_8']");

    $selectImp318.change(function () {

        if ($selectImp318.val() === "2") {
            $tbImp318.removeAttr("disabled");
            $tbImp318.focus();
        } else {
            $tbImp318.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr318 = $("[id$='select_str31_8']"),
        $tbStr318 = $("[id$='tb_str31_8']");

    $selectStr318.change(function () {

        if ($selectStr318.val() === "2") {
            $tbStr318.removeAttr("disabled");
            $tbStr318.focus();
        } else {
            $tbStr318.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp319 = $("[id$='select_imp31_9']"),
        $tbImp319 = $("[id$='tb_imp31_9']");

    $selectImp319.change(function () {

        if ($selectImp319.val() === "2") {
            $tbImp319.removeAttr("disabled");
            $tbImp319.focus();
        } else {
            $tbImp319.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr319 = $("[id$='select_str31_9']"),
        $tbStr319 = $("[id$='tb_str31_9']");

    $selectStr319.change(function () {

        if ($selectStr319.val() === "2") {
            $tbStr319.removeAttr("disabled");
            $tbStr319.focus();
        } else {
            $tbStr319.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp3110 = $("[id$='select_imp31_10']"),
        $tbImp3110 = $("[id$='tb_imp31_10']");

    $selectImp3110.change(function () {

        if ($selectImp3110.val() === "2") {
            $tbImp3110.removeAttr("disabled");
            $tbImp3110.focus();
        } else {
            $tbImp3110.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr3110 = $("[id$='select_str31_10']"),
        $tbStr3110= $("[id$='tb_str31_10']");

    $selectStr3110.change(function () {

        if ($selectStr3110.val() === "2") {
            $tbStr3110.removeAttr("disabled");
            $tbStr3110.focus();
        } else {
            $tbStr3110.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp32A = $("[id$='select_imp32_A']"),
        $tbImp32A = $("[id$='tb_imp32_A']");

    $selectImp32A.change(function () {

        if ($selectImp32A.val() === "2") {
            $tbImp32A.removeAttr("disabled");
            $tbImp32A.focus();
        } else {
            $tbImp32A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr32A = $("[id$='select_str32_A']"),
        $tbStr32A = $("[id$='tb_str32_A']");

    $selectStr32A.change(function () {

        if ($selectStr32A.val() === "2") {
            $tbStr32A.removeAttr("disabled");
            $tbStr32A.focus();
        } else {
            $tbStr32A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp32B = $("[id$='select_imp32_B']"),
        $tbImp32B = $("[id$='tb_imp32_B']");

    $selectImp32B.change(function () {

        if ($selectImp32B.val() === "2") {
            $tbImp32B.removeAttr("disabled");
            $tbImp32B.focus();
        } else {
            $tbImp32B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr32B = $("[id$='select_str32_B']"),
        $tbStr32B = $("[id$='tb_str32_B']");

    $selectStr32B.change(function () {

        if ($selectStr32B.val() === "2") {
            $tbStr32B.removeAttr("disabled");
            $tbStr32B.focus();
        } else {
            $tbStr32B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp32C = $("[id$='select_imp32_C']"),
        $tbImp32C = $("[id$='tb_imp32_C']");

    $selectImp32C.change(function () {

        if ($selectImp32C.val() === "2") {
            $tbImp32C.removeAttr("disabled");
            $tbImp32C.focus();
        } else {
            $tbImp32C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr32C = $("[id$='select_str32_C']"),
        $tbStr32C = $("[id$='tb_str32_C']");

    $selectStr32C.change(function () {

        if ($selectStr32C.val() === "2") {
            $tbStr32C.removeAttr("disabled");
            $tbStr32C.focus();
        } else {
            $tbStr32C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp32D = $("[id$='select_imp32_D']"),
        $tbImp32D = $("[id$='tb_imp32_D']");

    $selectImp32D.change(function () {

        if ($selectImp32D.val() === "2") {
            $tbImp32D.removeAttr("disabled");
            $tbImp32D.focus();
        } else {
            $tbImp32D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr32D = $("[id$='select_str32_D']"),
        $tbStr32D = $("[id$='tb_str32_D']");

    $selectStr32D.change(function () {

        if ($selectStr32D.val() === "2") {
            $tbStr32D.removeAttr("disabled");
            $tbStr32D.focus();
        } else {
            $tbStr32D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp32E = $("[id$='select_imp32_E']"),
        $tbImp32E = $("[id$='tb_imp32_E']");

    $selectImp32E.change(function () {

        if ($selectImp32E.val() === "2") {
            $tbImp32E.removeAttr("disabled");
            $tbImp32E.focus();
        } else {
            $tbImp32E.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr32E = $("[id$='select_str32_E']"),
        $tbStr32E = $("[id$='tb_str32_E']");

    $selectStr32E.change(function () {

        if ($selectStr32E.val() === "2") {
            $tbStr32E.removeAttr("disabled");
            $tbStr32E.focus();
        } else {
            $tbStr32E.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp32F = $("[id$='select_imp32_F']"),
        $tbImp32F = $("[id$='tb_imp32_F']");

    $selectImp32F.change(function () {

        if ($selectImp32F.val() === "2") {
            $tbImp32F.removeAttr("disabled");
            $tbImp32F.focus();
        } else {
            $tbImp32F.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr32F = $("[id$='select_str32_F']"),
        $tbStr32F = $("[id$='tb_str32_F']");

    $selectStr32F.change(function () {

        if ($selectStr32F.val() === "2") {
            $tbStr32F.removeAttr("disabled");
            $tbStr32F.focus();
        } else {
            $tbStr32F.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp32G = $("[id$='select_imp32_G']"),
        $tbImp32G = $("[id$='tb_imp32_G']");

    $selectImp32G.change(function () {

        if ($selectImp32G.val() === "2") {
            $tbImp32G.removeAttr("disabled");
            $tbImp32G.focus();
        } else {
            $tbImp32G.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr32G = $("[id$='select_str32_G']"),
        $tbStr32G = $("[id$='tb_str32_G']");

    $selectStr32G.change(function () {

        if ($selectStr32G.val() === "2") {
            $tbStr32G.removeAttr("disabled");
            $tbStr32G.focus();
        } else {
            $tbStr32G.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp33A = $("[id$='select_imp33_A']"),
        $tbImp33A = $("[id$='tb_imp33_A']");

    $selectImp33A.change(function () {

        if ($selectImp33A.val() === "2") {
            $tbImp33A.removeAttr("disabled");
            $tbImp33A.focus();
        } else {
            $tbImp33A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr33A = $("[id$='select_str33_A']"),
        $tbStr33A = $("[id$='tb_str33_A']");

    $selectStr33A.change(function () {

        if ($selectStr33A.val() === "2") {
            $tbStr33A.removeAttr("disabled");
            $tbStr33A.focus();
        } else {
            $tbStr33A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp33B = $("[id$='select_imp33_B']"),
        $tbImp33B = $("[id$='tb_imp33_B']");

    $selectImp33B.change(function () {

        if ($selectImp33B.val() === "2") {
            $tbImp33B.removeAttr("disabled");
            $tbImp33B.focus();
        } else {
            $tbImp33B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr33B = $("[id$='select_str33_B']"),
        $tbStr33B = $("[id$='tb_str33_B']");

    $selectStr33B.change(function () {

        if ($selectStr33B.val() === "2") {
            $tbStr33B.removeAttr("disabled");
            $tbStr33B.focus();
        } else {
            $tbStr33B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp33C = $("[id$='select_imp33_C']"),
        $tbImp33C = $("[id$='tb_imp33_C']");

    $selectImp33C.change(function () {

        if ($selectImp33C.val() === "2") {
            $tbImp33C.removeAttr("disabled");
            $tbImp33C.focus();
        } else {
            $tbImp33C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr33C = $("[id$='select_str33_C']"),
        $tbStr33C = $("[id$='tb_str33_C']");

    $selectStr33C.change(function () {

        if ($selectStr33C.val() === "2") {
            $tbStr33C.removeAttr("disabled");
            $tbStr33C.focus();
        } else {
            $tbStr33C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp33D = $("[id$='select_imp33_D']"),
        $tbImp33D = $("[id$='tb_imp33_D']");

    $selectImp33D.change(function () {

        if ($selectImp33D.val() === "2") {
            $tbImp33D.removeAttr("disabled");
            $tbImp33D.focus();
        } else {
            $tbImp33D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr33D = $("[id$='select_str33_D']"),
        $tbStr33D = $("[id$='tb_str33_D']");

    $selectStr33D.change(function () {

        if ($selectStr33D.val() === "2") {
            $tbStr33D.removeAttr("disabled");
            $tbStr33D.focus();
        } else {
            $tbStr33D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp33E = $("[id$='select_imp33_E']"),
        $tbImp33E = $("[id$='tb_imp33_E']");

    $selectImp33E.change(function () {

        if ($selectImp33E.val() === "2") {
            $tbImp33E.removeAttr("disabled");
            $tbImp33E.focus();
        } else {
            $tbImp33E.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr33E = $("[id$='select_str33_E']"),
        $tbStr33E = $("[id$='tb_str33_E']");

    $selectStr33E.change(function () {

        if ($selectStr33E.val() === "2") {
            $tbStr33E.removeAttr("disabled");
            $tbStr33E.focus();
        } else {
            $tbStr33E.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp33F = $("[id$='select_imp33_F']"),
        $tbImp33F = $("[id$='tb_imp33_F']");

    $selectImp33F.change(function () {

        if ($selectImp33F.val() === "2") {
            $tbImp33F.removeAttr("disabled");
            $tbImp33F.focus();
        } else {
            $tbImp33F.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr33F = $("[id$='select_str33_F']"),
        $tbStr33F = $("[id$='tb_str33_F']");

    $selectStr33F.change(function () {

        if ($selectStr33F.val() === "2") {
            $tbStr33F.removeAttr("disabled");
            $tbStr33F.focus();
        } else {
            $tbStr33F.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp34A = $("[id$='select_imp34_A']"),
        $tbImp34A = $("[id$='tb_imp34_A']");

    $selectImp34A.change(function () {

        if ($selectImp34A.val() === "2") {
            $tbImp34A.removeAttr("disabled");
            $tbImp34A.focus();
        } else {
            $tbImp34A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr34A = $("[id$='select_str34_A']"),
        $tbStr34A = $("[id$='tb_str34_A']");

    $selectStr34A.change(function () {

        if ($selectStr34A.val() === "2") {
            $tbStr34A.removeAttr("disabled");
            $tbStr34A.focus();
        } else {
            $tbStr34A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp34B = $("[id$='select_imp34_B']"),
        $tbImp34B = $("[id$='tb_imp34_B']");

    $selectImp34B.change(function () {

        if ($selectImp34B.val() === "2") {
            $tbImp34B.removeAttr("disabled");
            $tbImp34B.focus();
        } else {
            $tbImp34B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr34B = $("[id$='select_str34_B']"),
        $tbStr34B = $("[id$='tb_str34_B']");

    $selectStr34B.change(function () {

        if ($selectStr34B.val() === "2") {
            $tbStr34B.removeAttr("disabled");
            $tbStr34B.focus();
        } else {
            $tbStr34B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp34C = $("[id$='select_imp34_C']"),
        $tbImp34C = $("[id$='tb_imp34_C']");

    $selectImp34C.change(function () {

        if ($selectImp34C.val() === "2") {
            $tbImp34C.removeAttr("disabled");
            $tbImp34C.focus();
        } else {
            $tbImp34C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr34C = $("[id$='select_str34_C']"),
        $tbStr34C = $("[id$='tb_str34_C']");

    $selectStr34C.change(function () {

        if ($selectStr34C.val() === "2") {
            $tbStr34C.removeAttr("disabled");
            $tbStr34C.focus();
        } else {
            $tbStr34C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp4A = $("[id$='select_imp4_A']"),
        $tbImp4A = $("[id$='tb_imp4_A']");

    $selectImp4A.change(function () {

        if ($selectImp4A.val() === "2") {
            $tbImp4A.removeAttr("disabled");
            $tbImp4A.focus();
        } else {
            $tbImp4A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr4A = $("[id$='select_str4_A']"),
        $tbStr4A = $("[id$='tb_str4_A']");

    $selectStr4A.change(function () {

        if ($selectStr4A.val() === "2") {
            $tbStr4A.removeAttr("disabled");
            $tbStr4A.focus();
        } else {
            $tbStr4A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp4B = $("[id$='select_imp4_B']"),
        $tbImp4B = $("[id$='tb_imp4_B']");

    $selectImp4B.change(function () {

        if ($selectImp4B.val() === "2") {
            $tbImp4B.removeAttr("disabled");
            $tbImp4B.focus();
        } else {
            $tbImp4B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr4B = $("[id$='select_str4_B']"),
        $tbStr4B = $("[id$='tb_str4_B']");

    $selectStr4B.change(function () {

        if ($selectStr4B.val() === "2") {
            $tbStr4B.removeAttr("disabled");
            $tbStr4B.focus();
        } else {
            $tbStr4B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp4C = $("[id$='select_imp4_C']"),
        $tbImp4C = $("[id$='tb_imp4_C']");

    $selectImp4C.change(function () {

        if ($selectImp4C.val() === "2") {
            $tbImp4C.removeAttr("disabled");
            $tbImp4C.focus();
        } else {
            $tbImp4C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr4C = $("[id$='select_str4_C']"),
        $tbStr4C = $("[id$='tb_str4_C']");

    $selectStr4C.change(function () {

        if ($selectStr4C.val() === "2") {
            $tbStr4C.removeAttr("disabled");
            $tbStr4C.focus();
        } else {
            $tbStr4C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp4D = $("[id$='select_imp4_D']"),
        $tbImp4D = $("[id$='tb_imp4_D']");

    $selectImp4D.change(function () {

        if ($selectImp4D.val() === "2") {
            $tbImp4D.removeAttr("disabled");
            $tbImp4D.focus();
        } else {
            $tbImp4D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr4D = $("[id$='select_str4_D']"),
        $tbStr4D = $("[id$='tb_str4_D']");

    $selectStr4D.change(function () {

        if ($selectStr4D.val() === "2") {
            $tbStr4D.removeAttr("disabled");
            $tbStr4D.focus();
        } else {
            $tbStr4D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp4E = $("[id$='select_imp4_E']"),
        $tbImp4E = $("[id$='tb_imp4_E']");

    $selectImp4E.change(function () {

        if ($selectImp4E.val() === "2") {
            $tbImp4E.removeAttr("disabled");
            $tbImp4E.focus();
        } else {
            $tbImp4E.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr4E = $("[id$='select_str4_E']"),
        $tbStr4E = $("[id$='tb_str4_E']");

    $selectStr4E.change(function () {

        if ($selectStr4E.val() === "2") {
            $tbStr4E.removeAttr("disabled");
            $tbStr4E.focus();
        } else {
            $tbStr4E.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp4F = $("[id$='select_imp4_F']"),
        $tbImp4F = $("[id$='tb_imp4_F']");

    $selectImp4F.change(function () {

        if ($selectImp4F.val() === "2") {
            $tbImp4F.removeAttr("disabled");
            $tbImp4F.focus();
        } else {
            $tbImp4F.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr4F = $("[id$='select_str4_F']"),
        $tbStr4F = $("[id$='tb_str4_F']");

    $selectStr4F.change(function () {

        if ($selectStr4F.val() === "2") {
            $tbStr4F.removeAttr("disabled");
            $tbStr4F.focus();
        } else {
            $tbStr4F.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp4G = $("[id$='select_imp4_G']"),
        $tbImp4G = $("[id$='tb_imp4_G']");

    $selectImp4G.change(function () {

        if ($selectImp4G.val() === "2") {
            $tbImp4G.removeAttr("disabled");
            $tbImp4G.focus();
        } else {
            $tbImp4G.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr4G = $("[id$='select_str4_G']"),
        $tbStr4G = $("[id$='tb_str4_G']");

    $selectStr4G.change(function () {

        if ($selectStr4G.val() === "2") {
            $tbStr4G.removeAttr("disabled");
            $tbStr4G.focus();
        } else {
            $tbStr4G.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp51A = $("[id$='select_imp51_A']"),
        $tbImp51A = $("[id$='tb_imp51_A']");

    $selectImp51A.change(function () {

        if ($selectImp51A.val() === "2") {
            $tbImp51A.removeAttr("disabled");
            $tbImp51A.focus();
        } else {
            $tbImp51A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr51A = $("[id$='select_str51_A']"),
        $tbStr51A = $("[id$='tb_str51_A']");

    $selectStr51A.change(function () {

        if ($selectStr51A.val() === "2") {
            $tbStr51A.removeAttr("disabled");
            $tbStr51A.focus();
        } else {
            $tbStr51A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp51B = $("[id$='select_imp51_B']"),
        $tbImp51B = $("[id$='tb_imp51_B']");

    $selectImp51B.change(function () {

        if ($selectImp51B.val() === "2") {
            $tbImp51B.removeAttr("disabled");
            $tbImp51B.focus();
        } else {
            $tbImp51B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr51B = $("[id$='select_str51_B']"),
        $tbStr51B = $("[id$='tb_str51_B']");

    $selectStr51B.change(function () {

        if ($selectStr51B.val() === "2") {
            $tbStr51B.removeAttr("disabled");
            $tbStr51B.focus();
        } else {
            $tbStr51B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp52A = $("[id$='select_imp52_A']"),
        $tbImp52A = $("[id$='tb_imp52_A']");

    $selectImp52A.change(function () {

        if ($selectImp52A.val() === "2") {
            $tbImp52A.removeAttr("disabled");
            $tbImp52A.focus();
        } else {
            $tbImp52A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr52A = $("[id$='select_str52_A']"),
        $tbStr52A = $("[id$='tb_str52_A']");

    $selectStr52A.change(function () {

        if ($selectStr52A.val() === "2") {
            $tbStr52A.removeAttr("disabled");
            $tbStr52A.focus();
        } else {
            $tbStr52A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp52B = $("[id$='select_imp52_B']"),
        $tbImp52B = $("[id$='tb_imp52_B']");

    $selectImp52B.change(function () {

        if ($selectImp52B.val() === "2") {
            $tbImp52B.removeAttr("disabled");
            $tbImp52B.focus();
        } else {
            $tbImp52B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr52B = $("[id$='select_str52_B']"),
        $tbStr52B = $("[id$='tb_str52_B']");

    $selectStr52B.change(function () {

        if ($selectStr52B.val() === "2") {
            $tbStr52B.removeAttr("disabled");
            $tbStr52B.focus();
        } else {
            $tbStr52B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp52C = $("[id$='select_imp52_C']"),
        $tbImp52C = $("[id$='tb_imp52_C']");

    $selectImp52C.change(function () {

        if ($selectImp52C.val() === "2") {
            $tbImp52C.removeAttr("disabled");
            $tbImp52C.focus();
        } else {
            $tbImp52C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr52C = $("[id$='select_str52_C']"),
        $tbStr52C = $("[id$='tb_str52_C']");

    $selectStr52C.change(function () {

        if ($selectStr52C.val() === "2") {
            $tbStr52C.removeAttr("disabled");
            $tbStr52C.focus();
        } else {
            $tbStr52C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp53A = $("[id$='select_imp53_A']"),
        $tbImp53A = $("[id$='tb_imp53_A']");

    $selectImp53A.change(function () {

        if ($selectImp53A.val() === "2") {
            $tbImp53A.removeAttr("disabled");
            $tbImp53A.focus();
        } else {
            $tbImp53A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr53A = $("[id$='select_str53_A']"),
        $tbStr53A = $("[id$='tb_str53_A']");

    $selectStr53A.change(function () {

        if ($selectStr53A.val() === "2") {
            $tbStr53A.removeAttr("disabled");
            $tbStr53A.focus();
        } else {
            $tbStr53A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp53B = $("[id$='select_imp53_B']"),
        $tbImp53B = $("[id$='tb_imp53_B']");

    $selectImp53B.change(function () {

        if ($selectImp53B.val() === "2") {
            $tbImp53B.removeAttr("disabled");
            $tbImp53B.focus();
        } else {
            $tbImp53B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr53B = $("[id$='select_str53_B']"),
        $tbStr53B = $("[id$='tb_str53_B']");

    $selectStr53B.change(function () {

        if ($selectStr53B.val() === "2") {
            $tbStr53B.removeAttr("disabled");
            $tbStr53B.focus();
        } else {
            $tbStr53B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp53C = $("[id$='select_imp53_C']"),
        $tbImp53C = $("[id$='tb_imp53_C']");

    $selectImp53C.change(function () {

        if ($selectImp53C.val() === "2") {
            $tbImp53C.removeAttr("disabled");
            $tbImp53C.focus();
        } else {
            $tbImp53C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr53C = $("[id$='select_str53_C']"),
        $tbStr53C = $("[id$='tb_str53_C']");

    $selectStr53C.change(function () {

        if ($selectStr53C.val() === "2") {
            $tbStr53C.removeAttr("disabled");
            $tbStr53C.focus();
        } else {
            $tbStr53C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp53D = $("[id$='select_imp53_D']"),
        $tbImp53D = $("[id$='tb_imp53_D']");

    $selectImp53D.change(function () {

        if ($selectImp53D.val() === "2") {
            $tbImp53D.removeAttr("disabled");
            $tbImp53D.focus();
        } else {
            $tbImp53D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr53D = $("[id$='select_str53_D']"),
        $tbStr53D = $("[id$='tb_str53_D']");

    $selectStr53D.change(function () {

        if ($selectStr53D.val() === "2") {
            $tbStr53D.removeAttr("disabled");
            $tbStr53D.focus();
        } else {
            $tbStr53D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp54A = $("[id$='select_imp54_A']"),
        $tbImp54A = $("[id$='tb_imp54_A']");

    $selectImp54A.change(function () {

        if ($selectImp54A.val() === "2") {
            $tbImp54A.removeAttr("disabled");
            $tbImp54A.focus();
        } else {
            $tbImp54A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr54A = $("[id$='select_str54_A']"),
        $tbStr54A = $("[id$='tb_str54_A']");

    $selectStr54A.change(function () {

        if ($selectStr54A.val() === "2") {
            $tbStr54A.removeAttr("disabled");
            $tbStr54A.focus();
        } else {
            $tbStr54A.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp54B = $("[id$='select_imp54_B']"),
        $tbImp54B = $("[id$='tb_imp54_B']");

    $selectImp54B.change(function () {

        if ($selectImp54B.val() === "2") {
            $tbImp54B.removeAttr("disabled");
            $tbImp54B.focus();
        } else {
            $tbImp54B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr54B = $("[id$='select_str54_B']"),
        $tbStr54B = $("[id$='tb_str54_B']");

    $selectStr54B.change(function () {

        if ($selectStr54B.val() === "2") {
            $tbStr54B.removeAttr("disabled");
            $tbStr54B.focus();
        } else {
            $tbStr54B.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp54C = $("[id$='select_imp54_C']"),
        $tbImp54C = $("[id$='tb_imp54_C']");

    $selectImp54C.change(function () {

        if ($selectImp54C.val() === "2") {
            $tbImp54C.removeAttr("disabled");
            $tbImp54C.focus();
        } else {
            $tbImp54C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr54C = $("[id$='select_str54_C']"),
        $tbStr54C = $("[id$='tb_str54_C']");

    $selectStr54C.change(function () {

        if ($selectStr54C.val() === "2") {
            $tbStr54C.removeAttr("disabled");
            $tbStr54C.focus();
        } else {
            $tbStr54C.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
    var $selectImp54D = $("[id$='select_imp54_D']"),
        $tbImp54D = $("[id$='tb_imp54_D']");

    $selectImp54D.change(function () {

        if ($selectImp54D.val() === "2") {
            $tbImp54D.removeAttr("disabled");
            $tbImp54D.focus();
        } else {
            $tbImp54D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state

    var $selectStr54D = $("[id$='select_str54_D']"),
        $tbStr54D = $("[id$='tb_str54_D']");

    $selectStr54D.change(function () {

        if ($selectStr54D.val() === "2") {
            $tbStr54D.removeAttr("disabled");
            $tbStr54D.focus();
        } else {
            $tbStr54D.attr("disabled", "disabled").val("");
        }
    }).trigger("change"); // added trigger to calculate initial state
    //----------------------------------------------------------------//
});